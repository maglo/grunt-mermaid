language: node_js
node_js:
- "node"

install:
- npm install

script:
- npm pack
# And now test that the sample can run
- cd sample
- npm install
- npm install ../grunt-mermaid-0.9.0.tgz
- npm install grunt
- npm install grunt-cli -g
- grunt mermaid:default
- grunt mermaid:other

deploy:
  provider: npm
  email: "jballe@users.noreply.github.com"
  on:
    tags: true
    
   